{
  "name": "@cef-ux/design-tokens",
  "version": "0.1.0",
  "description": "Design Tokens universais (cores, tipografia, espaçamento, sombras) para o Context Engineering Framework UX.",
  "main": "tokens.ts",
  "types": "tokens.ts",
  "license": "MIT",
  "exports": {
    ".": {
      "import": "./tokens.ts",
      "require": "./tokens.ts"
    }
  },
  "scripts": {
    "build": "echo 'No build required — pure tokens.'",
    "lint": "eslint . --ext .ts,.json",
    "format": "prettier --write ."
  },
  "keywords": ["design-tokens", "tailwind", "cef", "ux", "react", "vue"],
  "devDependencies": {
    "eslint": "^9.8.0",
    "prettier": "^3.3.3"
  }
}
